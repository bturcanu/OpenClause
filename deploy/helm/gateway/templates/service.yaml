apiVersion: v1
kind: Service
metadata:
  name: {{ include "aag-gateway.fullname" . }}
  labels:
    {{- include "aag-gateway.labels" . | nindent 4 }}
spec:
  type: ClusterIP
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "aag-gateway.selectorLabels" . | nindent 4 }}
