apiVersion: v1
kind: Service
metadata:
  name: {{ include "aag-approvals.fullname" . }}
  labels:
    {{- include "aag-approvals.labels" . | nindent 4 }}
spec:
  type: ClusterIP
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "aag-approvals.selectorLabels" . | nindent 4 }}
