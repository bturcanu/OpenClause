apiVersion: v1
kind: Service
metadata:
  name: {{ include "oc-connector-jira.fullname" . }}
  labels:
    {{- include "oc-connector-jira.labels" . | nindent 4 }}
spec:
  type: ClusterIP
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "oc-connector-jira.selectorLabels" . | nindent 4 }}
