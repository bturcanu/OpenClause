apiVersion: v1
kind: Service
metadata:
  name: {{ include "aag-connector-jira.fullname" . }}
  labels:
    {{- include "aag-connector-jira.labels" . | nindent 4 }}
spec:
  type: ClusterIP
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "aag-connector-jira.selectorLabels" . | nindent 4 }}
