apiVersion: v1
kind: Service
metadata:
  name: {{ include "oc-connector-slack.fullname" . }}
  labels:
    {{- include "oc-connector-slack.labels" . | nindent 4 }}
spec:
  type: ClusterIP
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "oc-connector-slack.selectorLabels" . | nindent 4 }}
