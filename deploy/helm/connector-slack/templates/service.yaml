apiVersion: v1
kind: Service
metadata:
  name: {{ include "aag-connector-slack.fullname" . }}
  labels:
    {{- include "aag-connector-slack.labels" . | nindent 4 }}
spec:
  type: ClusterIP
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "aag-connector-slack.selectorLabels" . | nindent 4 }}
